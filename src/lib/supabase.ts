// Re-export the project's Supabase browser client for code that imports
// from `@/lib/supabase`. The canonical client lives at
// `@/integrations/supabase/client`.
export { supabase } from "@/integrations/supabase/client";
